# Supabase Configuration
# Retrieve these from your Supabase Project Dashboard -> Settings -> API

# ==========================================
# Deployment Instructions (Production)
# ==========================================
# Since .env.local is not committed, you must manually set these keys in your CI/CD provider:
#
# 1. Vercel (Frontend & API):
#    Go to: Settings -> Environment Variables
#    Add: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY (and Service Role Key only if needed by API Routes)
#
# 2. GitHub Actions (CI & Cron Jobs):
#    Go to: Repo Settings -> Secrets and variables -> Actions
#    Add: 
#      - NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY (For CI 'pnpm build')
#      - SUPABASE_SERVICE_ROLE_KEY (For scripts/cron jobs)
#
# ==========================================

# The public URL of your Supabase project (Safe to expose to browser)
NEXT_PUBLIC_SUPABASE_URL=

# The anonymous key (Safe to expose, limited by RLS policies)
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# The service role key (backend only, bypasses RLS - DO NOT SHARE OR COMMIT)
# Required for running scripts
SUPABASE_SERVICE_ROLE_KEY=
